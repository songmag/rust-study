[package]
name = "algorithm"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level=0
[profile.release]
opt-level=3
# opt-level = 0 ~ 3 최적화 속도
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
